# 研究日志：无力矩传感器力矩估计（首个可运行原型）

> 这是本项目的第一份**真实研究产出**。它记录了一次完整的"假设—失败—排查—修正"过程。
> 论文的"方法"和"实验设计"两节，可以直接从这里长出来。
>
> 运行方式：`python src/simulate_stdlib.py`（零第三方依赖，任何 Python 3 都能跑）

---

## 一、已确认成立的结论（可写进论文）

### 1. 无力矩传感器的力矩估计框架有效

在只有相电流 + 编码器、没有关节力矩传感器的条件下：

| 方法 | 接触段力矩估计 RMSE |
|---|---|
| 纯电流开环（`τ = K_t·i_q`） | 0.174 N·m |
| 动量观测器残差直接用（不扣摩擦） | 0.186 N·m |
| **观测器 + 摩擦补偿（本方法）** | **0.030 N·m** |

**相对纯电流基线改善 82.6%（最好一组）/ 71.0%（最差一组）。**

这是本项目最硬、最可复现的结论。

### 2. 稳态下观测器残差精确等于摩擦力

实测（恒速段）：

| 真实 `q̇` | 观测器残差 `r` | 真实 `τ_f` | 误差 |
|---|---|---|---|
| −0.338 rad/s | −0.1562 | −0.1569 | 0.4% |
| −0.301 rad/s | −0.1564 | −0.1563 | 0.1% |
| −0.187 rad/s | −0.1557 | −0.1561 | 0.3% |

吻合到 1% 以内。这验证了理论推导：`ṙ = K_O(τ_f + τ_ext − r) ⇒ r → τ_f + τ_ext`。

### 3. "扣除已辨识摩擦"这一步是必要的

直接用残差 `r` 当力矩估计（不扣摩擦）时 RMSE = 0.186，是扣摩擦后的 **6 倍**。
说明摩擦补偿不是可有可无的装饰，而是方法的必要组成。

---

## 二、尚未解决的核心问题（论文要攻的地方）

### Stribeck 参数辨识不稳定

三组不同激励协议下辨识出的参数：

| 组 | 激励速度范围 | `τ_c` 误差 | `τ_s` 误差 | `b` 误差 | `v_s` 误差 |
|---|---|---|---|---|---|
| A 只有任务运动 | ±2.25 rad/s | 7.7% | 74.6% | 44.3% | 94.0% |
| B 补低速段 | ±0.30 rad/s | 4.0% | 21.3% | 48.3% | 110.1% |
| C 补全速域 | ±2.00 rad/s | 8.8% | 50.7% | 43.0% | 94.0% |

**问题表现**：
- `v_s` 在三组里全部偏离真值（0.05），且**都倾向搜索边界**（0.003 或 0.105）
- `τ_s` 与 `b` 系统性偏低
- 拟合在自身数据上很好（RMSE 0.013~0.023），**但参数值不唯一**

**诊断**：这是典型的 `(τ_s, v_s)` 参数退化 —— **曲线形状拟合对了，但参数不对**。
低速段数据能约束 `τ_c`，但 `τ_s` 与 `v_s` 在有限速度范围内高度相关，无法分别确定。

**这实际上是可接受的**：下游只需要 `τ_f(q̇)` 曲线准确，不需要参数物理正确。
但这一点必须被明确验证和论证，而不是含糊过去。

---

## 三、被推翻的假设（诚实记录）

> **原假设**：覆盖全速域的自激励协议（C 组）应优于只覆盖低速（B 组）。

**实测结果：不成立。** A 组（82.6%）≈ C 组（81.7%）> B 组（71.0%）。
三组差异不大，且 A 组（完全没有专门激励）表现最好。

**含义**：激励设计的收益没有我预期的那么大，说明瓶颈不在激励覆盖范围，
而在**辨识算法本身**（固定 `v_s` + 网格搜索 + 线性最小二乘）。
下一步应该把力气花在算法上（EKF），而不是继续调激励轨迹。

**这个否定结果本身有价值** —— 它避免了在错误的方向上继续投入。

---

## 四、方法学教训（踩过的坑，值得写进论文）

### 坑：采样窗必须避开速度切换的过渡段

**现象**：加了低速自激励后，辨识精度反而**恶化** ——
`τ_c` 误差从 4% 恶化到 57%，`b` 变成 **−0.26**（负黏滞，物理上不可能）。

**排查路径**（5 轮）：
1. 怀疑拟合算法 → 加岭正则化 + 归一化 Gram 行列式检验 + 限定 `v_s` 区间 → 无效
2. 怀疑模型 → 把数据按速度分箱打印 → 发现残差走势对不上
3. 怀疑速度跟踪 → 打印实际轨迹 → **找到根因**

**根因**：参考速度是**阶跃切换**的，关节需要约 0.15 s 减速到位。
原采样窗从段内 35% 开始，此时**过渡尚未结束**。
过渡样本（`q̇≈0` 但 `r` 仍带着上一段的摩擦值）与模型 `τ_f(0)=0` 严重矛盾，
把最小二乘彻底带偏。

**修复**：段时长 0.25~0.30 s → **0.5 s**；采样窗 35%~85% → **60%~98%**。
修复后稳态残差标准差仅 0.0005 N·m。

**推广**：任何基于"稳态关系"的在线辨识，都必须显式处理暂态污染。
这不是实现细节，是方法可行性的一部分。

---

## 五、代码结构

```
src/
  joint_model.py        单关节机电耦合模型（dq简化 + Stribeck摩擦）—— 真值系统
  momentum_observer.py  广义动量观测器（含稳定性条件检查）
  friction_rls.py       RLS 在线摩擦辨识 + v_s 网格搜索
  simulate.py           numpy/matplotlib 版（出论文用图）
  simulate_stdlib.py    零依赖版 ← **已验证可运行**，含 A/B/C 三组对照
docs/figures/
  metrics_stdlib.txt    完整指标报告
  trace.csv             轨迹数据（可用 Excel/Matlab 画图）
```

## 六、下一步（按优先级）

1. **EKF 在线估计 `v_s`** —— 当前把 `v_s` 当固定超参数、在网格上离散选择，
   这是参数退化的直接原因
2. **明确验证"曲线精度 vs 参数精度"** —— 论证下游真正需要的是前者
3. **引入温度效应** —— `K_t` 与 `R_s` 随温度漂移，很可能是当前偏差来源之一
4. **降阶模型退路** —— 若仍不稳定，只辨识 `τ_c` 与 `b`，把 Stribeck 项作为保守上界
5. **接入 MATLAB/Simulink** —— 复现同样的结果，作为论文的主实现环境

---

## 六·补、「完美摩擦悖论」—— 本项目最有价值的发现

### 现象

在接触段比较四种力矩估计（数值为 RMSE，单位 N·m）：

| 估计方式 | RMSE |
|---|---|
| 纯电流开环 `τ = K_t·i_q` | 0.174 |
| 观测器残差 `r` 直接用（不扣摩擦） | 0.186 |
| **观测器 + 已辨识摩擦补偿** | **0.030** ← 最好 |
| 观测器 + **真实摩擦**补偿（"完美"） | **0.066** ← 反而最差 |

**用真实摩擦模型去补偿，效果比用辨识出来的模型差一倍以上。**

这违反直觉：如果辨识出的参数是错的（`τ_s` 误差 75%、`v_s` 误差 94%），
为什么它比真值还好用？

### 排查：三个候选解释，两个被排除

**① 测量噪声？→ 排除。**

| 测速噪声 | K_O | 本方法 | 完美摩擦 |
|---|---|---|---|
| 5e-4 | 80 | 0.03020 | 0.06607 |
| 2e-4 | 80 | 0.03038 | 0.06607 |
| **0** | 80 | **0.03053** | **0.06613** |

把噪声降到零，结果几乎不变 → **纯系统性效应，与噪声无关。**

**② 观测器动态滞后？→ 部分成立，只解释一半。**

| K_O | 时间常数 | 本方法 | 完美摩擦 | 差距 |
|---|---|---|---|---|
| 80 | 12.5 ms | 0.03020 | 0.06607 | 0.03587 |
| 200 | 5.0 ms | 0.02778 | 0.05431 | 0.02653 |
| 400 | 2.5 ms | 0.03435 | 0.05222 | 0.01786 |
| 1500 | 0.67 ms | 0.03470 | 0.05189 | 0.01719 |

带宽提高后差距从 0.036 收敛到 0.017，但**不归零**，在 ~0.017 处触底。
而且本方法的 RMSE 并不随带宽单调改善（0.0302 → 0.0347，反而略差）
—— 说明存在**带宽 / 噪声的权衡**，`K_O` 是一个需要整定的设计参数。

**③ 剩余的系统性偏差 → 这是真正的机理。**

### 机理判断（待进一步验证）

自由运动段里，观测器残差 `r` 是**经过观测器动态滤波的**摩擦力，
而不是瞬时真实摩擦力。用 `r` 去拟合摩擦模型时，拟合出的 `τ̂_f`
逼近的是**"观测器看到的摩擦"**，而不是真值。

于是：
- 辨识与补偿走的是**同一个滤波器** → 系统性误差互相抵消
- 代入真实摩擦反而**打破了这个一致性** → 误差变大

**这恰好是"自监督"这个命名最本质的含义**：辨识的目标不是物理真值，
而是**观测器视角下的等效摩擦**，而这正是补偿时真正需要减掉的东西。

### 旁证：为什么"只用任务运动"的 A 组表现最好

| 组 | 激励 | 力矩 RMSE | 改善 |
|---|---|---|---|
| A | 只用任务运动（多正弦） | 0.03020 | **82.6%** |
| B | 补低速阶梯（≤0.3 rad/s） | 0.05051 | 71.0% |
| C | 补全速域阶梯（≤2.0 rad/s） | 0.03585 | 81.7% |

A 组没有任何专门的辨识激励，却表现最好。按上述机理这就说得通了：
**A 组的辨识与估计处在完全相同的工况下**，系统性误差最大程度抵消。
B 组在低速辨识、高速使用，工况不匹配 → 外推误差最大。

**推论**：与其精心设计"标准"摩擦辨识实验，不如**在与任务相同的工况下辨识**。
这个结论与摩擦辨识文献的常规做法相反，值得作为论文的核心论点之一。

---

## 六·再补、跨任务稳健性检验（4 个任务 × 4 种辨识方式）

单一任务的结论不够硬，所以在 **4 个不同任务**上重复检验。
接触段对每个任务都是同一个外部力矩，各方式的"纯电流基线"必须一致（内置公平性检查，全部通过）。

| 任务 | in-regime | proto-B | proto-C | oracle(真值) |
|---|---|---|---|---|
| T1 多正弦 0.3/1.1 Hz | **0.02912** | 0.04590 | 0.02966 | 0.05831 |
| T2 单频 0.5 Hz | **0.02129** | 0.03710 | 0.02260 | 0.04489 |
| T3 慢多正弦 0.1/0.7 Hz | 0.02845 | 0.04159 | **0.02823** | 0.05604 |
| T4 调频 0.2→1.5 Hz | **0.02046** | 0.07966 | 0.02542 | 0.02926 |

（数值为接触段力矩估计 RMSE，单位 N·m，越小越好）

### 三条结论，按稳健性排序

**① 「完美摩擦悖论」在 4/4 个任务上成立。**
oracle（用真实摩擦模型补偿）在**每一个**任务上都显著差于辨识模型，
差距 4%~100%。这是本项目最稳健的发现。

**② proto-B（只覆盖低速）在 4/4 个任务上最差。**
差 37%~289%。**只做低速辨识是不够的** —— 这条结论跨任务一致。

**③ in-regime 与 proto-C 几乎持平（3/4 微弱胜出，差距 0.8%~5%）。**

### 我修正了自己的假设（第二次）

> **上一轮的表述**："辨识必须与使用工况相同"（in-regime 优于一切）。
> **本轮实测**：in-regime ≈ proto-C。二者只差 0.8%~5%，T3 上 proto-C 还赢了。

**所以正确的、更弱的表述是**：

> 起作用的是**辨识数据要覆盖实际工作的速度区间**，
> 而不是"必须用任务本身"。
> 用任务自身做辨识是保证覆盖的一种**零成本方式**（不需要额外协议），
> 但设计一个覆盖全速域的阶梯协议效果几乎相同。

这个修正很重要 —— 原表述会被审稿人用"你只是碰巧在某个任务上赢了"直接驳回。
**收窄后的表述是可以站住的。**

### 工程含义（对实际落地最有价值的一条）

不需要为每个新任务重新做辨识。**一次覆盖全速域的阶梯激励（几秒钟的位置指令），
就能在所有任务上取得接近最优的补偿效果。**
而且 proto-B 的反例说明：**省掉高速段会付出 37%~289% 的代价。**

---

## 六·三补、误差预算：剩余误差来自哪里（含两级自监督辨识）

### 动机

前面测出接触段 RMSE ≈ 0.029，而摩擦模型曲线误差只有 ≈ 0.010。
**剩余的部分不是摩擦模型贡献的**，来源未知。已排除噪声（降为 0 无变化），
部分归因于观测器动态延迟（提高 K_O 只解释一半）。

### 检验两个真实存在的效应

| 效应 | 无补偿 RMSE | 齿槽补偿后 | 改善 | 纯电流基线 |
|---|---|---|---|---|
| 无附加效应 | 0.02912 | 0.02868 | 1.5% | 0.17917 |
| **齿槽转矩**（幅值 0.02 N·m） | 0.03387 | **0.02852** | **15.8%** | 0.17845 |
| **K_t 温漂 10%** | 0.03541 | 0.03395 | 4.1% | 0.18001 |
| 两者兼有 | 0.04160 | **0.03395** | **18.4%** | 0.17995 |

### 误差贡献分解（相对无附加效应的增量，平方和开根）

```
齿槽转矩贡献        = 0.01731 N*m
温漂贡献            = 0.02015 N*m
独立叠加预测        = 0.02656 N*m
实际两者兼有的增量  = 0.02971 N*m
```

预测与实际接近 → 两个误差源近似独立，可用平方和合成。
**两者合起来使误差从 0.029 恶化到 0.042，几乎翻倍。**

### ✅ 正面结果：齿槽转矩可以零成本补偿

齿槽转矩只与**位置**相关，因此在自由运动段的残差里，它是**可重复、可辨识**的。
做法：从自由段残差中减去已辨识的摩擦分量，剩下的按位置分箱平均，
得到一张位置索引的查找表（48 箱），在接触段前馈补偿。

**效果**：齿槽转矩单独存在时，RMSE 从 0.03387 恢复到 **0.02852**
——与"无附加效应"的 0.02912 基本持平，即**残差被完全消除**。

**这形成了两级自监督辨识**：
```
第一级：速度相关的摩擦      → 残差 vs 速度  → 拟合 Stribeck 曲线
第二级：位置相关的齿槽转矩  → 残差残差 vs 位置 → 位置查找表
两级都只用自由运动段的观测器残差，不需要任何额外传感器或测力台。
```

### ❌ 温漂无法用查找表解决

温漂是**与电流成正比的乘性误差**（`Δτ = K_t·i_q·drift·(1−e^{−t/τ})`），
不是位置的函数，所以位置查找表拿它没办法（只改善 4.1%，属于搭便车）。

**必须用在线参数辨识单独处理** —— 工业伺服里成熟的做法是
**d 轴电流注入法**在线辨识定子电阻 `R_s` 与磁链 `ψ_f`，从而实时修正 `K_t`。
纯算法，不需要额外硬件，非常适合只有现成驱动器的条件。

### 对真实硬件的实验优先级（这是本节最有用的产出）

1. **先做温漂补偿** —— 收益大（0.020）、成本低（纯算法）、实现独立
2. **再做齿槽补偿** —— 收益中（0.017）、需要一个位置索引表、但要先能精确回零定位
3. 最后再抠摩擦模型 —— 收益最小（0.010），而且已证明参数退化对下游无害

---

## 六·四补、设计敏感度分析（第三次被数据推翻的假设）

选择了"自己造关节"这条路，问题就变成：**该选什么电机、编码器、减速器？**
本节的目的是给出定量依据。

| 扫描变量 | 结果 |
|---|---|
| **测速噪声** 1e-2 → 1e-4 | 改善率稳定 **80% 左右**，绝对 RMSE 几乎不变（0.033~0.035） |
| **观测器增益** K_O | **100 rad/s 在全部噪声水平下最优**；K_O≥400 明显恶化（0.046~0.048） |
| **摩擦水平** 0.25× → 4× | 改善率 80%~88%，**最低摩擦时反而最高（87.8%）** |
| **等效惯量** 0.01 → 0.1 | 本方法 RMSE 基本不变；基线从 0.177 恶化到 1.160，改善率 **80% → 98%** |

### ⚠️ 我在写脚本时预先写了四条"结论"，其中三条被数据推翻

这是本项目**第三次**发生同样的事（前两次：激励设计假设、"必须同工况"假设）。
教训已经很明显：**结论必须从数据读出，不能先写后验。**

被推翻的三条：
1. ❌ 我以为"噪声越低越好、编码器最关键" → 实际噪声影响**很弱**，两个数量级只带来微小变化
2. ❌ 我以为"最优 K_O 随噪声变化" → 实际**恒为 100**，与噪声无关
3. ❌ 我以为"摩擦越大方法优势越大" → 实际**低摩擦时改善率最高**
4. ❌ 我以为"惯量影响小" → 实际惯量影响**很大**（但方向有利）

### 修正后的设计准则（可用于采购决策）

**① 编码器质量不是精度瓶颈 —— 这是最大的意外，也是好消息。**
噪声从 1e-2 降到 1e-4（两个数量级）几乎不改变结果。
→ **低成本编码器 + 2 ms 低通就够了，预算不要花在这里。**
⚠️ 未解异常：极端低噪声（1e-5）时反而变差（0.0453）。可能是无噪声时最小二乘
过拟合于系统性模型偏差。**列为待查项**，不要写进论文当作结论。

**② 观测器增益按 100 rad/s 起步，不要盲目追高带宽。**
K_O=100 在所有噪声水平下最优；K_O≥400 显著恶化。
→ `K_O·T_s = 0.01`，时间常数 10 ms。这是可以直接写进论文的整定规则。

**③ 方法在"大惯量、高负载"场合价值最大。**
J 增大时基线急剧恶化而本方法稳定，改善率升到 98%。
→ 这修正了论文的**卖点定位**：不该说"适合高摩擦"，该说
**"适合大惯量、重载、且没有力矩传感器的场合"**。

**④ 摩擦水平不是卖点。** 改善率对摩擦几乎不敏感（80~88%），别拿它做论证。

---

## 六·五补、温漂补偿：两种做法被否定，一种成立

误差预算指出温漂贡献 0.020 N·m，是剩余误差里最大的一项，且**位置查找表无效**
（它是与电流成正比的乘性误差 `Δτ = K_t·i_q·δ(t)`）。

### 被否定的做法 ①：接触段门控在线自适应

**结果：发散。** δ̂ 跑到 0.1385（真值 0.0753），RMSE 反而**恶化 17.6%**。

根因两条：
- 步长 γ=0.02 配 0.1 ms 采样周期，等效增益高了约三个数量级
- **更根本**：接触段**不存在**可用于学习的"无接触窗口"。门控只在外部力矩
  过零的极短瞬间开放，此时更新步长相对该窗口过大 → 振荡。

> **结论：漂移必须在自由段学好，接触段不许在线学习。**

### 四种模式对比

| 模式 | 接触段 RMSE | 相对无补偿 |
|---|---|---|
| 不补偿 | 0.03395 | — |
| 自由段平均 δ（批量最小二乘） | 0.03267 | 3.7% |
| **RLS 末值（保持）** | **0.02979** | **12.3%** |
| 热模型外推 δ(t)=A(1−e^{−t/τ}) | 0.03147 | 7.3% |

### 被否定的做法 ②：热模型外推（**与预期相反**）

我预期"δ(t) 有明确的物理形式，拟合热模型外推应该最好"。**实测不如简单地保持
RLS 末值。**

原因：RLS 轨迹本身有滞后，用它拟合出一阶热模型的**幅值与时间常数都偏低**
（拟合在 t=4 s 给出 δ̂=0.0254，真值 0.0551）。外推把偏差放大了。

> **教训：先验证估计器的跟踪质量，再谈模型外推。**
> 用一个滞后估计器的输出去拟合动态模型，只会把滞后变成系统性偏差。

### 为什么 RLS 末值反而最好（诚实说明）

RLS 在 t=4 s 给出 δ̂=0.0693，而真值只有 0.0551 —— **它高估了 26%**。
但接触段 δ 继续上升到 0.0753，所以这个"高估"恰好抵消了保持常数带来的低估。
**这个成功有一部分是巧合，不是设计出来的**，论文里应如实说明。

### 最终状态

| 误差源 | 贡献 | 补偿手段 | 实测改善 |
|---|---|---|---|
| 齿槽转矩 | 0.0173 | 位置查找表（自由段残差离线提取） | **15.8%（几乎完全消除）** |
| 温漂 | 0.0202 | 自由段 RLS 估计 δ，接触段保持 | **12.3%（部分消除）** |

两者都不需要任何额外传感器。**至此，"识别出的主要误差源都有可行补偿"这条
论文主张可以成立了** —— 但温漂那一项的残余必须在论文里如实交代。

---

## 附录：论文用图（零依赖生成）

图由 `src/make_figures.py` 生成 —— **纯标准库直接输出 SVG**，不需要
numpy/matplotlib（本机装不上，用户的机器也未必有）。所有图通过 XML 合法性检查。

| 文件 | 内容 |
|---|---|
| `fig1_observer.svg` | 自由段残差 vs 真实摩擦；接触段无力矩传感器力矩估计 |
| `fig2_rmse_bars.svg` | 四种估计方式的接触段 RMSE 对比 |
| `fig3_cross_task.svg` | 4 任务 × 3 种辨识工况 |
| `fig4_error_budget.svg` | 误差预算：齿槽 / 温漂，补偿前后 |

**生成过程中发现并修正的两个问题**（值得记录）：
1. fig3 的任务标签原本是中文 —— SVG 用的 Helvetica 无中文字形，
   且论文是英文。已改为英文标签。
2. 图例裕量只有 1.18~1.22 倍，会压住柱子。已提高到 1.55 倍。

**验证方式**：解析 SVG 抽取全部 `<text>` 与 `<polyline>`，核对
①无中文残留 ②数值与实测一致（如 fig2 的 0.1740/0.1856/0.0302/0.0661）
③折线点数合理。**"能被 XML 解析"不等于"画对了"，所以必须核对内容。**

---

## 六·六补、模型失配鲁棒性（回应"循环论证"质疑）

### 为什么必须做

此前所有仿真的隐患：**真实对象与辨识模型用的是同一个 Stribeck 模型**。
这正是审稿人一定会打的点 —— *"你在用同一个模型既生成数据又拟合数据"*。

### [1] 摩擦模型失配：真实=LuGre，辨识=Stribeck

| 真实对象 | 本方法 RMSE | 纯电流基线 | 改善 |
|---|---|---|---|
| Stribeck（匹配） | 0.02912 | 0.17917 | 83.7% |
| **LuGre（失配）** | **0.04785** | 0.17879 | **73.2%** |

**劣化 +64.3%，但相对基线仍改善 73.2%。**

> ✅ **方法不是循环论证** —— 辨识模型与真实对象不一致时依然显著有效。

**关键设计**：`σ₂ = b` 使 LuGre 的**稳态**摩擦曲线与 Stribeck **完全相同**，
所以测的是**纯动态失配**（迟滞与预滑移），这是最难拟合的一类。
静态参数误差会被辨识吸收，相对好处理。

### [2] 惯量失配：不对称，且方向出乎意料

| `J_true/J_nom` | 本方法 RMSE | 改善 |
|---|---|---|
| 0.80 | 0.02624 | 84.9% |
| 0.90 | 0.02637 | 85.0% |
| 1.00 | 0.02912 | 83.7% |
| 1.10 | 0.03990 | 78.1% |
| 1.25 | 0.04636 | 75.4% |

⚠️ **低估 J 反而略优于精确值；高估 J 明显恶化。原因未明，列为待查项。**
不写成结论 —— 可能只是该工况下的巧合。

### 对真实硬件的具体含义

1. **惯量参数需在装配后实测标定**。`ΔJ·q̈` 直接进入残差，且与加速度相关，
   不易与摩擦分离 —— 这是一个明确的实验步骤要求。
2. **摩擦模型不必追求完美**：即使真实对象是 LuGre 级别，Stribeck 仍够用（73%）。
3. 想从 73% 提到 84% 才需要上 LuGre 辨识 —— **收益递减**，
   应排在温漂与齿槽补偿**之后**。

---

## 七、这份日志的用途

- **套磁信附件**：证明你不是"只会考试"，而是能独立走完一轮研究闭环
- **面试素材**：导师问"你做过什么研究"，你可以讲这个 5 轮排查的故事
- **论文骨架**：第四节可以直接改写成论文的"实验设计"章节
- **诚实性证明**：里面明确写了"我的假设被推翻"。**这比任何吹嘘都有说服力。**
