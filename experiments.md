# 实验记录

主程序 `src/simulate_stdlib.py`，其余脚本对应下面各个实验。所有数字都可以重跑复现。

## 1. 框架有效性

自由段做辨识，接触段加外部力矩，看能不能估出来。任务 T1。

    纯电流 τ = Kt·iq          0.174 N·m
    残差直接用（不扣摩擦）     0.186
    观测器 + 摩擦补偿          0.030
    用真实摩擦模型补偿         0.066

第 2 行说明必须扣摩擦，扣与不扣差 6 倍。第 4 行是意外：用真实摩擦反而更差。

稳态（恒速段）下观测器残差和真实摩擦的对比：

    真实 q̇ = -0.338 rad/s    r = -0.1562    真值 -0.1569
    真实 q̇ = -0.301          r = -0.1564    真值 -0.1563
    真实 q̇ = -0.187          r = -0.1557    真值 -0.1561

偏差在 1% 以内。

## 2. 不同辨识协议的对比（regime_study.py）

4 个任务 × 3 种辨识方式：

    任务        in-regime   低速协议   全速域协议   真实摩擦
    T1 多正弦     0.0291     0.0459     0.0297      0.0583
    T2 单频       0.0213     0.0371     0.0226      0.0449
    T3 慢多正弦   0.0285     0.0416     0.0282      0.0560
    T4 调频       0.0205     0.0797     0.0254      0.0293

只覆盖低速的协议在 4 个任务上都是最差的，差 1.4~2.9 倍。

本来假设"用任务自身做辨识"应该明显更好，实测它和全速域协议差不多（差 0.8~5%）。
起作用的是辨识数据要覆盖实际工作的速度区间，不是非要用任务本身。

## 3. 摩擦参数的可辨识性（identify_compare.py）

    方法         τc 误差   τs 误差   b 误差   vs 误差   曲线 RMSE
    网格最小二乘   7.7%     74.6%    44.3%    94.0%    0.0103
    EKF          9.4%     32.4%    40.2%   783.5%    0.0099

τs 和 vs 在有限速度激励下无法分开确定，试了 EKF 也没解决，vs 反而更差。
但力矩曲线的误差只有 0.010 N·m 左右，约为库仑摩擦的 6.7%。
补偿用的是曲线，不是这三个参数本身，所以这个退化影响不大。

## 4. 误差预算（error_budget.py）

    效应              无补偿     齿槽补偿后
    无附加效应        0.02912    0.02868
    齿槽转矩          0.03387    0.02852
    温漂 10%          0.03541    0.03395
    两者兼有          0.04160    0.03395

齿槽转矩贡献 0.0173，温漂贡献 0.0202，两者合起来把误差从 0.029 抬到 0.042。

齿槽转矩只和位置有关，所以在自由段的残差里是可重复的，可以按位置分箱做成查找表。
补偿之后回到 0.0285，和没有齿槽时的 0.0291 基本一样。

温漂是按电流成比例的乘性误差，位置查找表没用，要单独处理。

## 5. 温漂补偿（temp_comp.py）

    不补偿                     0.03395
    自由段平均 δ（最小二乘）    0.03267
    RLS 末值保持               0.02979
    一阶热模型外推             0.03147
    接触段门控在线自适应        0.03916

最后一行是失败的尝试，δ 估到 0.1385（真值 0.0753），发散。
原因是步长相对 0.1 ms 采样周期太大，而且接触段本来就没有无接触窗口可以用来学习。
漂移只能在自由段学好。

热模型外推本来以为会最好，实测不如直接保持 RLS 末值。因为 RLS 轨迹本身有滞后，
拟合出来的幅值和时间常数都偏低。教训是先用估计器自己的跟踪质量检验一遍，
再去拟合动态模型。

RLS 末值能赢有一部分是巧合：它在 t=4s 给出 0.0693，真值 0.0551，是偏高的，
而接触段 δ 继续上升，正好抵消了保持常数带来的偏低。

## 6. 模型失配（mismatch_study.py）

前面的仿真里真实对象和辨识模型用的是同一个 Stribeck 模型。这里让真实对象换成 LuGre，
辨识仍然用 Stribeck。把 σ2 取成黏滞系数，使两者的稳态摩擦曲线完全相同，
所以测的是纯动态失配。

    真实 Stribeck    0.02912
    真实 LuGre       0.04785

劣化 64%，但相对纯电流基线仍然改善 73.2%。

惯量失配是不对称的：

    J_true/J_nom    0.80     0.90     1.00     1.10     1.25
    RMSE           0.02624  0.02637  0.02912  0.03990  0.04636

低估 J 反而略好，高估明显变差。原因没搞清楚。

## 7. 参数敏感性（design_study.py）

    测速噪声 1e-2 ~ 1e-4    改善率稳定在 80% 左右，几乎不变
    观测器增益 K_O          100 rad/s 在各噪声水平下都最优，400 以上明显变差
    摩擦水平 0.25x ~ 4x     改善率 80~88%，低摩擦时反而略高
    惯量 J 0.01 -> 0.1      基线从 0.177 恶化到 1.160，本方法基本不变

噪声的影响比预期小很多，说明编码器质量不是精度瓶颈。
这一节里我事先写了四条结论，其中三条被数据推翻。

## 没搞清楚的

- 极低噪声（1e-5）时精度反而下降
- 惯量失配为什么不对称
- 温漂只能补偿一部分
- 观测器残差与真实摩擦不符的机理只有推测，没验证
- 全部结果来自仿真，没有硬件数据

## 踩过的坑

**采样窗必须避开速度切换的过渡段。** 加低速激励之后精度反而恶化，
τc 误差从 4% 变成 57%，b 变成 -0.26（负的黏滞系数，物理上不可能）。
查了五轮才找到原因：参考速度是阶跃切换的，关节要 0.15 s 才减速到位，
而采样窗从段内 35% 开始，采到的还是过渡段。这些点的 q̇ 接近 0 但残差还带着
上一段的摩擦值，和 τf(0)=0 严重矛盾。把段长改成 0.5 s、窗口改到 60%~98% 之后好了。
